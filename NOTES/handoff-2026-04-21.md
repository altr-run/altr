# Handoff — 2026-04-21

Planning session. No code shipped — all work is in `docs/landing-v1-plan.md`.

## What shipped

- **Landing v1 plan written** at `docs/landing-v1-plan.md`. Next.js + Sanity CMS port of `Altr Landing v7.html` (the one in `~/Downloads/altr.run/`).
- **Placeholder app icons** generated via `scripts/gen_icons.py` — warm linen tile + ochre Pax mark, rendered at 1024px and scaled to `32x32.png`, `128x128.png`, `128x128@2x.png`, `icon.png`, plus multi-size `.ico` and `.icns`. Unblocks `tauri build` and `tauri dev`.

## Key decisions from this session

1. **Landing base = SanityPress** ([nuotsu/sanitypress-with-typegen](https://github.com/nuotsu/sanitypress-with-typegen) — the typed fork). Not a hand-rolled Next.js scaffold.
   - Next.js 15 App Router + Sanity v3 + Tailwind 4 + React 19, all pre-wired.
   - Embedded Studio at `/admin` (SanityPress v4 convention; moved from `/studio` in the main repo).
   - OSS / free. Pro ($10/mo) only needed for i18n; we don't need it in v1.
2. **All content in Sanity.** Every string on the page — hero, ticker, pricing tiers, playground prompts+responses, FAQ, stamp, footer legal — is a schema field. Mukul edits at `/admin`, no redeploys for copy.
3. **Waitlist submissions stay on Turso + Resend.** Never written to Sanity. Sanity is for published content, not PII.
4. **Monorepo restructure** into bun workspaces: `apps/desktop/` (current root) + `apps/landing/` (new) + `packages/tokens/`. Forked tokens: `tokens-base.css` shared, `tokens-desktop.css` (linen+ochre) + `tokens-landing.css` (white+amber) diverge until the palettes are reconciled.

## v7 → SanityPress module map (see plan for full sketch)

| v7 section | Module | Built-in or custom |
|---|---|---|
| Nav, Footer | `site` singleton | built-in |
| Hero (centered) | `hero.saas` | built-in |
| Product Overview §01 | `flag-list` | built-in |
| Pricing | `pricing-list` + 3× `pricing.tier` docs | built-in |
| FAQ | `accordion-list` | built-in |
| Close CTA | `callout` | built-in |
| Ticker | `ticker.scrolling` | **custom** |
| Playground | `playground.prompts` | **custom** |
| Stamp | `stamp.mark` | **custom** |
| Waitlist form | `form.waitlist` (copy only; POSTs to `/api/waitlist`) | **custom** |

## Next session target

**PR #1 of the landing migration: monorepo scaffold.** Concrete steps, in order:

1. Add `packages/tokens/` — copy `src/styles/tokens.css` to `packages/tokens/tokens-base.css`, create `tokens-desktop.css` and `tokens-landing.css` forks, add `packages/tokens/package.json` + `index.ts`.
2. Add root `package.json` workspaces manifest: `{ "private": true, "workspaces": ["apps/*", "packages/*"] }` with `desktop:dev` / `landing:dev` / `lint` / `typecheck` scripts.
3. Verify `bun install` still works (single lockfile at root).
4. Do **not** move desktop yet — that's PR #2. This PR is pure additive.

Est ~2h. Desktop dev loop must be unaffected at the end.

Then PR #2 in the same Saturday session: `git mv src → apps/desktop/src`, `git mv src-tauri → apps/desktop/src-tauri`, update `tauri.conf.json` paths, update `.github/workflows/lite.yml` working directories. Verify `bun run desktop:dev` end-to-end.

PRs #3–#5 (SanityPress scaffold, built-in module ports, custom modules + waitlist API) are scheduled as their own two-week push after desktop Week-1 work lands.

## Week-1 desktop work (parallel track — don't forget)

Once the monorepo move is done, start the graph store in Rust at `apps/desktop/src-tauri/src/graph/`:
- Typed wrappers over the `nodes` table from migration 0001.
- A tokio broadcast channel for graph events (replayed from the `events` append-only table on startup).
- First real `#[tauri::command]`: `graph_create_project`.
- TanStack Query wrapper on the TS side.

## Open questions (from the landing plan — punt on these for PR #1 + #2)

1. Is `a\tr` the real wordmark or placeholder? Lands in Sanity `site` singleton + favicon + OG image before PR #3 can be signed off.
2. Who owns the Sanity org? Create under `hello@altr.run`, not personal — transferability.
3. `altr.run` DNS — who controls the zone? Is `staging.altr.run` available?
4. Resend + Turso accounts also under `hello@altr.run`.
5. Do we keep SanityPress's default `/blog` scaffolding unlinked, or strip it in PR #3? Plan says strip; confirm.
6. Palette reconciliation — when we do the real logo pass, force landing's amber into desktop too? Defer to v0.1 RC.

## Things I deliberately did NOT do

- No implementation yet. Mukul needs to sign off on the revised plan + SanityPress pick before PR #1 starts.
- No Sanity org created. That's Mukul's account setup.
- No changes to `CLAUDE.md` §4 file layout — waits until PR #2 lands and the move is real.
- No license pick. Both `package.json` and `Cargo.toml` still say `UNLICENSED`. Punt to v0.1 release prep.
