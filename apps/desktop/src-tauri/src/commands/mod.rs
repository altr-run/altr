//! All `#[tauri::command]` handlers. One file per feature.
//!
//! Invocation contract: every command takes typed params, returns
//! `Result<T, AppError>`. See CLAUDE.md §4 + §7.

// pub mod projects;
// pub mod specs;
// pub mod tickets;
// pub mod agents;
// pub mod settings;
